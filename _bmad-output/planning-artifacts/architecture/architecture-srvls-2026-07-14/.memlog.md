---
scope: One-host Runtime Promise lifecycle, Host discovery, reconciliation, morning Brief, lifecycle control, and reversible Rust delivery
purpose: Canonical build substrate for implementation epics, stories, coding agents, and the readiness gate
altitude: feature
updated: 2026-07-16T11:30
---

- (constraint) [ADOPTED] Preserve the existing command surface and flat JSON six-field record, Prometheus metric families, Markdown/table behavior, inspect semantics, and stop/restart/start/disable mappings during the Rust migration; additions must be backward-compatible.
- (constraint) [ADOPTED] Rust and ratatui are user-selected. Default presentation groups related entries by inferred stack and uses strategic color and icons.
- (constraint) The utility remains Linux-first, local-only, single-process, best-effort across cron, systemd system/user, Docker, and PM2; it has no daemon, server, network API, database, or durable application state.
- (direction) Fast path selected. Produce a terse build substrate plus implementation-ready epics and stories for coding agents. Assumptions may be made and must be tagged until reality-checked.
- (version) Verified live on 2026-07-14: ratatui 0.30.2 is current; official release notes confirm the 0.30 modular workspace and ratatui::run lifecycle API. crates.io reports MSRV 1.85 for the 0.30 line.
- (version) Verified live on 2026-07-14 via crates.io index: clap 4.6.1, serde 1.0.228, serde_json 1.0.150, thiserror 2.0.18, tracing 0.1.44, tracing-subscriber 0.3.23, strsim 0.11.1, insta 1.48.0. Local Rust toolchain is rustc/cargo 1.85.1.
- (decision) Use hexagonal architecture for host integration boundaries plus a unidirectional Elm-style Model/Event/Update/View loop for ratatui. Binds all modules; prevents collectors, renderers, and TUI state from owning conflicting domain behavior.
- (decision) Define one provider-neutral Entry aggregate composed from stable identity, display name, kind/scope, status/health, optional schedule, provenance, capabilities, provider metadata, observations, and diagnostics. Provider-specific adapters compose facets into Entry; no inheritance-shaped type hierarchy. Binds collectors, grouping, renderers, and actions; prevents six incompatible provider models.
- (decision) Ports live inward and adapters implement Collector, Inspector, ActionExecutor, and CommandRunner. Domain imports no ratatui, clap, serde wire-format, subprocess, or provider modules. Binds dependency direction; prevents infrastructure and presentation concerns from leaking into domain policy.
- (decision) Stack inference is a Strategy pipeline producing evidence-scored candidate keys: explicit provider-native metadata first, normalized source/location second, conservative name similarity last. A deterministic resolver selects the highest-confidence group, exposes confidence/reasons, and leaves ambiguous entries ungrouped. Binds default grouping; prevents ad-hoc renderer heuristics and false-positive stacks.
- (decision) Semantic name grouping requires at least three members sharing a non-generic normalized prefix of at least two tokens; replica/instance numbers and provider suffixes may be stripped, but environment words and arbitrary substrings may not be silently discarded. Provider-native and shared-source evidence may form smaller groups. Binds similarity strategy; prevents accidental grouping of unrelated generic services.
- (decision) All inventory collection returns InventorySnapshot containing entries plus typed diagnostics. Collectors run concurrently behind bounded deadlines; failure or privilege denial is visible in TUI and stderr, and --strict converts any collector error to nonzero exit. Binds collection and errors; prevents missing subsystems from looking like a healthy empty host.
- (decision) Interactive action flow uses Command objects executed only through ActionExecutor after capability checks; destructive disable/delete requires a TUI confirmation, while direct CLI verbs preserve current explicit-command behavior. Every subprocess uses argv arrays, timeouts, output caps, and no shell. Binds lifecycle mutation; prevents duplicated unsafe command construction and shell injection.
- (decision) When stdout is a TTY, bare srvls opens ratatui; when non-TTY, bare srvls emits the legacy table. --table, --json, --prom, and --md are non-interactive and never emit ANSI, icons, or logs to stdout. Binds mode selection; prevents automation breakage and corrupted machine output.
- (decision) Centralize visual semantics in Theme and IconSet. Status color is the primary signal, provider icon is secondary, text labels always remain; honor NO_COLOR and offer an ASCII icon set so meaning never depends on color or glyph support. Binds TUI widgets; prevents inconsistent styling and inaccessible status-only color.
- (decision) Keep the flat external EntryV1 serialization contract separate from the richer domain Entry. Export presenters map domain snapshots to existing JSON/Prometheus/Markdown/table contracts; grouped export is opt-in and versioned later. Binds compatibility outputs; prevents internal evolution from silently breaking scripts.
- (decision) Use one binary crate with module boundaries domain, application, ports, adapters, presentation, and cli. Introduce workspace crates only after an independently reusable component reaches the Rule of Three. Binds project structure; prevents premature multi-crate and trait abstraction overhead.
- (decision) Use synchronous Rust with scoped worker threads/channels for concurrent collectors and TUI refresh; do not adopt Tokio absent a real async I/O requirement. Binds execution model; prevents dual runtimes and needless async complexity.
- (decision) Tests are deterministic below the host boundary: parser fixtures, fake ports, grouping property/table tests, presenter golden files, and ratatui TestBackend snapshots. Live-host smoke tests remain opt-in integration tests; CI must not depend on installed Docker, PM2, systemd, sudo, or cron content. Binds verification; prevents environment-sensitive CI and UI regressions.
- (decision) Ship as a locked standalone Linux binary with Cargo.lock committed, MSRV 1.85, rustfmt/clippy/test gates, and release artifacts; preserve the current executable name and provide a migration shim only if packaging requires it. Binds build/deployment; prevents toolchain drift and install-path breakage.
- (assumption) [ASSUMPTION] v1 configuration is CLI flags and environment only; no configuration file is added until user-defined grouping rules or themes have a demonstrated need.
- (assumption) [ASSUMPTION] Unicode Nerd Font is not required; default icons use broadly supported Unicode symbols with ASCII fallback.
- (question) Deferred until implementation feedback: whether users need persistent grouping overrides for ambiguous services. Revisit after default inference is exercised on big-chungus fixtures.
- (event) Fast-path architecture decision capture completed; distillation and independent reviewer gate pending.
- (version) Correction after primary registry review: ratatui 0.30.2 declares rust-version 1.88.0. Adopt MSRV Rust 1.88 and test both MSRV and current stable; local 1.85.1 must be upgraded before implementation.
- (decision) Amend grouping contract: normalize names with Unicode lowercase; split on non-alphanumeric separators and letter-number boundaries; remove only provider suffix tokens service, timer, scope, target and terminal replica tokens matching instance/ordinal patterns; never discard environment tokens. Evidence tiers are absolute: provider-native 300, exact normalized source 200, semantic prefix 100. Lower tiers never outweigh higher. Within a tier choose more members, then longer prefix/source specificity, then lexicographically smallest canonical key. Groups do not merge transitively across candidate keys.
- (decision) Define StackGroupId as evidence-kind plus normalized evidence key, with percent-encoded UTF-8 canonical form. Labels derive from provider project/namespace, source basename, or joined semantic prefix; duplicate labels receive provider/source disambiguation. Group identity persists while its winning evidence key persists, not by member set.
- (decision) Define EntryId as a typed tuple (provider, scope, native locator, occurrence). Provider adapters own canonical native locators: systemd full unit name; Docker immutable full container ID; PM2 numeric process ID plus PM2_HOME scope; cron source plus zero-based physical line and hash of exact schedule/user/command with occurrence index for duplicates. Encoding is percent-encoded UTF-8 components and is process-visible but not a promised durable external ID in v1.
- (decision) Collection buckets merge in fixed legacy order cron, systemd-system services/timers, systemd-user services/timers, Docker, PM2; each bucket sorts by legacy type then name then EntryId. Completion order never affects snapshots. Presenters retain current provider/type ordering and formatting through golden compatibility fixtures.
- (decision) Each collector returns CollectorReport with generation, entries, outcome Success|Partial|Unavailable|Denied|TimedOut|Failed, and scoped diagnostics. Snapshot diagnostics own all diagnostics; Entry holds diagnostic IDs only. --strict exits nonzero for Partial, Denied, TimedOut, or Failed, not Unavailable for optional absent tools. Failed refreshes never carry old entries forward as current; the TUI retains the last good snapshot visibly marked stale while showing the new failed report.
- (decision) TUI refreshes have monotonic generation IDs; only the latest requested generation may replace the displayed snapshot. Selection and confirmation targets bind EntryId plus source snapshot generation, never row indexes. Revalidation must resolve the same current native identity immediately before mutation or abort as stale.
- (decision) CommandRunner owns child termination and reaping: hard deadline, byte caps, nonzero/signal/UTF-8/truncation result semantics, kill process group on timeout when supported, and always wait. A bounded worker pool limits collectors. Dropping a channel result only suppresses stale delivery and is never treated as cancellation.
- (decision) Groups are read-only projections in v1. Lifecycle actions target individual entries only. Public disable remains the CLI verb but resolves to typed provider-native intent classified as systemd disable, Docker stop, or destructive PM2 delete. TUI confirms stop and disable/delete, while start/restart do not; action result distinguishes executed-and-verified, executed-unverified, refused, stale, and failed.
- (decision) TUI actions follow plan, capability/authorization preflight, confirmation, identity revalidation, execute, verify via targeted refresh, and result. Intrinsic provider support and observed authorization are separate. The binary never elevates itself; root collection and system mutations use sudo -n. TUI never permits an interactive password prompt in raw mode.
- (decision) One RAII TerminalSession owned by the TUI entry boundary initializes ratatui 0.30.2 with Crossterm, restores raw mode/alternate screen/cursor on success, error, interrupt, and unwind, and installs tracing/panic handling before terminal entry. TERM=dumb, failed init, or stdin/stdout not both terminals falls back to legacy table unless --tui explicitly requires an error.
- (decision) Retain --fzf as a deprecated compatibility alias for the ratatui UI without requiring fzf; remove undocumented --fzf-lines. V1 TUI keys: q/Esc quit/back, r refresh, / filter, Enter inspect/expand, arrows or j/k navigate, Space expand/collapse, s stop, R restart, x disable/delete, and ? help. Modal confirmations capture target identity/generation; small terminals collapse detail before list.
- (decision) Sanitize all untrusted terminal text by replacing C0/C1 controls except tab/newline in bounded detail views; cap inspected bytes/lines before rendering. Export escaping follows each formal format and intentional safety changes are recorded in the compatibility ledger. Command adapters use -- where supported and reject leading-dash identifiers where the provider lacks safe end-of-options support.
- (decision) Ship x86_64-unknown-linux-gnu first on the big-chungus glibc baseline. Release tarball and SHA-256 contain one versioned binary. Install to ~/.local/lib/srvls/<version>/srvls and atomically repoint ~/.local/bin/srvls, retaining the prior version for rollback. Post-install smoke validates version and all redirected exports plus existing timer invocations.
- (decision) CI uses cargo fmt --check, cargo clippy --locked --all-targets -- -D warnings, cargo test --locked --all-targets, an MSRV 1.88 job, current stable job, and release-asset smoke. Cargo.lock is authoritative; dependency updates are deliberate and rerun compatibility goldens.
- (decision) Authoritative migration corpus is captured from the Python baseline before adapter replacement: provider fixtures for success/malformed/unavailable/denied/timeout plus golden CLI stdout, stderr, exit codes, ordering, JSON pretty formatting, Prometheus, Markdown, table, inspection, and action argv mapping. Intentional deviations live in a reviewed compatibility ledger.
- (event) Reviewer gate findings triaged; all blocker/high findings accepted for autofix, including MSRV correction, deterministic grouping/identity/order, refresh/terminal ownership, --fzf migration, action safety, and deployment operations.
- (decision) PM2 observed identity is PM2_HOME scope plus numeric process ID plus birth discriminator of created_at/pm_uptime origin and executable path/name fingerprint. Actions may invoke numeric ID only after current jlist revalidation matches the full observed identity.
- (decision) Grouping native evidence in v1: Docker uses Compose working directory when present, else Compose project; PM2 uses non-default namespace; systemd and cron emit no native group evidence. Source evidence exists only for Docker Compose working directory and PM2 cwd. Source paths are normalized lexically only: absolute, preserve case, collapse repeated separators and dot segments, reject parent traversal, strip trailing slash except root, never resolve symlinks; full path is key and basename is label.
- (decision) Grouping resolver algorithm: candidates have tier, specificity integer (path component count or semantic token count), canonical key, and sorted member IDs. Sort by tier desc, current unclaimed member count desc, specificity desc, key asc; greedily claim unassigned members. Recompute residual count at selection; form native/source groups with >=2 residual members and semantic groups with >=3; discard smaller residuals entirely. One entry belongs to at most one group.
- (decision) CollectorReport includes Availability Required|Optional and sub-operation reports tagged Required|Advisory. Shared reducer: all successful => Success; any usable entries plus any required/advisory fault => Partial; no usable entries selects highest severity Failed > TimedOut > Denied > Unavailable. Subsidiary diagnostics are never hidden. Strict fails Partial/Failed/TimedOut/Denied and Required Unavailable; Optional Unavailable alone does not fail.
- (decision) Compatibility order is legacy encounter order, not alphabetical: collector buckets fixed cron, system-systemd, user-systemd, Docker, PM2; each entry carries adapter encounter ordinal and merge preserves ordinal. Markdown alone retains legacy sort by type/name. The previous proposed bucket sort by type/name is rejected as a compatibility change.
- (decision) The singular compatibility authority is checked-in tests/compat: capture-baseline.sh, fixtures/<provider>/<case>, golden/<mode>, compatibility-ledger.md. Capture records tool versions and normalizes only declared volatile placeholders for timestamp/load/host paths; tests never recapture from the live host. Python and Rust run against the same fixture command runner.
- (decision) Provider verification predicates: systemd start active; stop inactive; restart same unit plus newer InvocationID or ExecMainStartTimestamp; disable UnitFileState not enabled. Docker start running; stop/disable not running; restart same container ID plus changed StartedAt. PM2 start online same birth; stop stopped same birth; restart same birth plus increased restart_time or newer pm_uptime; delete absence of the observed full identity. Verification timeout or unavailable collector yields ExecutedUnverified; identity replacement yields Stale; negative observed predicate yields Failed.
- (decision) Action verification uses OperationId separate from refresh generation. Its targeted report completes the operation result regardless of later global refresh; it updates global snapshot only if its refresh generation is still latest. Mutations cannot be initiated from a stale displayed snapshot.
- (decision) Terminal policy uses panic=unwind. signal-hook 0.4.4 converts SIGINT/SIGTERM into shutdown events handled by Update, allowing TerminalSession drop; SIGKILL and fatal synchronous signals are documented best-effort exceptions. Ctrl-C key also maps to shutdown.
- (decision) Privilege compatibility ledger: noninteractive collection remains sudo -n; TUI system mutation uses sudo -n to prevent raw-mode prompts; explicit non-TUI CLI system mutation preserves legacy interactive sudo because the terminal session is not active. Denial output/exit behavior is golden-tested for both lanes.
- (decision) ProcessResult is total: termination SpawnFailed(kind)|Exited(code)|Signaled(signal)|TimedOut, stdout/stderr CapturedText with UTF-8-lossy text, original byte count, truncated flag, duration, and redacted argv identity. Nonzero/signals are values interpreted by adapters, not runner errors; truncation and invalid UTF-8 emit diagnostics while parse policy remains adapter-owned.
- (decision) Initial release ABI is glibc 2.42, verified on big-chungus 2026-07-14 via getconf GNU_LIBC_VERSION. Build in a pinned Ubuntu image with glibc 2.42 and verify with readelf symbol-version scan plus smoke on big-chungus. Broader Linux portability is deferred.
- (decision) Installer protocol: stage checksum-verified binary and run export smoke before link mutation; accept existing ~/.local/bin/srvls only if absent, managed, or the known repo symlink unless --force. Create version directory, create a temporary symlink in ~/.local/bin, atomically rename it over srvls, and record previous resolved target. Failure before rename removes staging and preserves old link. rollback atomically repoints to recorded previous target and reruns smoke; existing foreign regular files/symlinks require explicit --force.
- (event) Second adversarial findings accepted and resolved with total contracts for grouping, collection, compatibility, action verification, terminal signals, privilege, process execution, ABI, and installation.
- (event) spine finalized after deterministic lint and two adversarial review passes
- (event) Architecture update resumed on 2026-07-16 to reconcile the finalized canonical PRD and UX contracts before regenerating epics and implementation readiness.
- (constraint) [ADOPTED] The final 2026-07-16 PRD and addendum own product scope and vocabulary; final DESIGN.md and EXPERIENCE.md own visual and behavioral contracts; the live Python CLI and smoke suite remain the compatibility oracle.
- (constraint) [ADOPTED] Preserve one Rust binary, hexagonal dependency direction, an Elm-style ratatui Model/Event/Update/View shell, and Strategy, Adapter, and Command seams unless canonical evidence directly contradicts their detailed rules.
- (decision) Keep the architecture at feature altitude so its stable AD identifiers govern the implementation epics and stories one level down; the sole architecture artifact is a build substrate for those downstream units and the readiness gate.
- (assumption) The configured persistent project-context.md is absent. For this update, live source, tests, repository documentation, canonical PRD and UX artifacts, existing architecture decisions, prior epics, and the 2026-07-15 readiness report provide the reality-checked context.
- (version) Verified live on 2026-07-16: official ratatui release 0.30.2 remains current and its workspace declares Rust edition 2024 with rust-version 1.88.0; official sources are https://github.com/ratatui/ratatui/releases/tag/ratatui-v0.30.2 and https://raw.githubusercontent.com/ratatui/ratatui/ratatui-v0.30.2/Cargo.toml.
- (version) Verified via crates.io API on 2026-07-16: clap 4.6.2, serde 1.0.228, serde_json 1.0.150, thiserror 2.0.18, tracing 0.1.44, tracing-subscriber 0.3.23, insta 1.48.0, signal-hook 0.4.4, and strsim 0.11.1. Clap 4.6.2 supersedes the prior 4.6.1 pin.
- (version) Verified on 2026-07-16: official Rust stable is 1.97.0 from https://blog.rust-lang.org/releases/latest/; the local host has rustc and cargo 1.95.0, x86_64, and glibc 2.42. CI must test declared MSRV 1.88 and the moving current-stable channel; local stable should be refreshed before implementation.
- (constraint) Live canonical-Host check on 2026-07-16: one Python compatibility invocation completed in 0.66 to 0.68 seconds per table, JSON, Prometheus, and Markdown mode; emitted sizes were 58,647, 95,266, 1,700, and 32,942 bytes respectively. Operational defaults must leave headroom for the UX fixture of 2,000 Observations while keeping capture and refresh bounded.
- (decision) Amend AD-2: RuntimePromise, Observation, ReconciliationFinding, Snapshot, and OperationRecord are distinct provider-neutral aggregates. Provider facets compose into Observation; legacy EntryV1 is only an outward compatibility projection. This binds ownership of declared intent versus observed truth and prevents one blended status or one provider-shaped model.
- (decision) Add durable-state ownership: one bundled SQLite database under the XDG state directory implements inward-owned Promise, lifecycle-event, Snapshot, baseline, and operation repositories. The adapter uses transactions, WAL, synchronous FULL, foreign keys, embedded forward migrations, directory mode 0700, file mode 0600, integrity validation, and per-version backup. No domain or presentation module issues SQL.
- (version) Verified live on 2026-07-16: rusqlite 0.40.1 exists and exposes a bundled feature that compiles and links SQLite into the application, fitting the one-binary constraint; uuid 1.24.0 supports sortable UUIDv7; toml 1.1.3+spec-1.1.0 supports Serde parsing. Official references: https://docs.rs/crate/rusqlite/latest/features, https://docs.rs/uuid/latest/uuid/, and https://docs.rs/toml/latest/toml/.
- (decision) PromiseId, SnapshotId, OperationId, and lifecycle EventId use UUIDv7 values; ObservationId remains a deterministic provider, scope, native-locator, birth-evidence tuple under amended AD-13. Caller-supplied operation identity is stored under a uniqueness constraint with actor and operation kind for idempotent Agent retries.
- (decision) Configuration is typed TOML with precedence built-in defaults, /etc/srvls/config.toml, XDG user config, explicit config file, SRVLS-prefixed environment, then CLI value flags. Every effective field retains the winning and overridden provenance chain. Unknown keys, duplicate semantic keys, invalid types, and out-of-range values fail before collection, state writes, TUI entry, or mutation; there is no v1 hot reload.
- (decision) Promise lifecycle is an application-owned state machine over append-only events and a transactional current projection. Same-boot Lease calculations use monotonic boot time; wall time is retained for display. A boot-ID change expires ephemeral ownership until renewed, wall-clock discontinuity never extends a Lease, persistent intent requires durable ownership, and close retains exactly one released, completed, or revoked reason.
- (decision) Reconciliation is one pure deterministic domain use case over an immutable policy, Promise projection, and collection Snapshot. It resolves lifecycle, evidence status, identity correlation, Promise outcome, all compatible Observation labels, attention rank, and Safe-to-stop Assessment in canonical PRD order; weak secondary evidence may expose candidates but never establish healthy truth or an action identity.
- (decision) Operational collection defaults: eight concurrent Collector scopes with valid range 1 through 16; hard per-Collector defaults cron 15 seconds, each systemd scope 15 seconds, Docker 30 seconds, PM2 20 seconds, and direct process 5 seconds, each configurable 1 through 120 seconds; aggregate refresh default 35 seconds with range 5 through 180 and invariant not below the largest effective Collector deadline.
- (decision) Operational capture defaults: CommandRunner captures at most 2 MiB independently for stdout and stderr with valid range 64 KiB through 16 MiB, counts original bytes before lossy decoding, marks truncation, and drains or discards excess without deadlock. Provider inspection renders at most 256 KiB and 500 lines combined by default, valid ranges 4 KiB through 2 MiB and 10 through 5,000 lines, using the earlier bound and disclosing it before content.
- (decision) Operational state defaults: retain Snapshots for 30 days and at most 1,000 records, configurable 1 through 365 days and 2 through 10,000 records, while pinning current truth and the Accepted Baseline; retain closed-Promise lifecycle events and operation history for 90 days and at most 10,000 events, configurable 7 through 3,650 days and 100 through 1,000,000 events, while preserving active Promise truth and the latest closure summary.
- (decision) Operational ownership defaults: an omitted ephemeral Lease is 12 hours with valid range 5 minutes through 30 days; declared renewal cadence defaults to 5 minutes with range 10 seconds through 1 hour; Heartbeat grace defaults to 5 minutes with range 0 through 1 hour. Lifecycle is heartbeat-late only after cadence plus grace and before Lease expiry; no grace extends the Lease.
- (decision) Operational finding defaults: stale policy uses a 24-hour positive no-use window configurable 5 minutes through 30 days and emits no stale label without supported activity evidence. Hot policy requires CPU at least 80 percent or RSS at least 25 percent of Host memory for three samples spanning at least two minutes; thresholds range 1 through 100 percent, sample count 1 through 12, and window 1 minute through 1 hour. Insufficient samples produce no hot claim.
- (decision) Operational action defaults: Provider execution deadline 60 seconds with range 1 through 600; post-action verification window 30 seconds with range 1 through 300 and poll interval 1 second with range 250 milliseconds through 5 seconds; signal cancellation allows 2 seconds for typed graceful cancellation then kills the process group and reaps within a 5-second total shutdown grace configurable 1 through 30 seconds. SQLite busy timeout defaults to 5 seconds with range 100 milliseconds through 30 seconds.
- (decision) Amend AD-6: Action Menu a is the complete discovery path and can start from a RuntimePromise with a supported Launch Mechanism even without an Observation. Stop and disable or delete require TUI confirmation; unknown safety requires the exact resolved verb; explicit non-TUI CLI verbs retain compatibility behavior. Execution emits exactly verified, executed-unverified, refused, timed-out, or failed under PRD precedence; stale identity is a refused reason before launch and replacement is executed-unverified after launch.
- (decision) Amend AD-14: the Elm Update owner implements the UX-IP-10 phase table. Submitted operations never detach in v1; q is unavailable while active, Esc only navigates after submit, and Ctrl-C, SIGINT, or SIGTERM request phase-specific typed cancellation, persistence of one truthful OperationRecord, bounded child reaping, and unconditional TerminalSession restoration. Repeated signals force the configured kill-and-reap path without rewriting a durable outcome.
- (decision) Amend AD-7 and AD-9: validate configuration before all side effects. Bare invocation routes to TUI only when stdin and stdout are terminals and TERM is not dumb; otherwise legacy table. Explicit brief --linear is the complete human screen-reader lane, JSON remains machine-facing, and legacy table, top-level JSON, Prometheus, Markdown, inspection, and explicit actions remain separate compatibility contracts. Unknown options fail nonzero as a recorded compatibility-ledger safety deviation; --fzf remains the ratatui alias and --fzf-lines is retired.
- (decision) Amend AD-12 and durable-state rules: install and upgrade smoke use an isolated temporary state directory; upgrade acquires the state lock, creates a verified pre-migration database backup, validates and transactionally applies embedded migrations, then atomically activates the binary. Any failed activation restores both prior link and matching state backup. Rollback is never binary-only once durable state exists.
- (decision) Amend AD-11: deterministic CI adds Promise lifecycle, boot and clock discontinuity, SQLite migration and crash recovery, configuration precedence and invalid values, reconciliation axes and classifications, Safe-to-stop truth table, retention, canonical operational limits, human-linear fixtures, all UX states and budgets, and operation signal/race tests. The Python CLI plus smoke suite remains the live compatibility lane, not the only oracle.
- (decision) Add direct-process support inside the existing Adapter seam: Observation identity is Linux boot ID plus PID plus process start time and executable or command fingerprint; the collector deduplicates Provider-owned child processes and srvls itself. Only identity-safe signal actions are supported; start or restart requires a declared Launch Mechanism and never reconstructs an arbitrary shell command.
- (correction by Team Argus) The canonical July 16 PRD and UX supersede the earlier no-durable-state, one-Entry ownership, old Collector vocabulary, old Action Outcome, color-primary, and unconditional --fzf-lines-removal assumptions. Durable SQLite truth, separate RuntimePromise and Observation aggregates, canonical scoped outcomes, text-primary communication, and ledger-gated compatibility decisions now govern.
- (event by Team Argus) Reconciled draft now preserves AD-1 through AD-15, adds AD-16 through AD-20, ARCH-LIM-1 through ARCH-LIM-15, ARCH-HOST-1, and explicit canonical contract traceability. Independent semantic and live-operation reports remain review inputs before finalization.
- (event by Team Argus) Final live-operations review d00b91a incorporated. The report ratifies the one-binary hexagonal and Elm-style paradigm, freezes exact Python compatibility edge cases, and requires the operational contract below before implementation.
- (correction by Team Argus) Supersede earlier collection and capture defaults: collection.max_concurrency is 4 range 1 through 8; the eight scope deadlines are Docker 30 seconds, PM2 20, system and user systemd 15 each, cron user/root/system 10 each, and process 10. Deterministic LPT yields a 35-second default makespan; scheduler margin 5 seconds yields generation cutoff 40 seconds. Separate stdout and stderr caps apply at child 4 MiB and 256 KiB, scope 8 MiB and 512 KiB, and generation 32 MiB and 2 MiB.
- (correction by Team Argus) Supersede earlier state and retention defaults: Snapshots retain 14 days and 256 historical records; events retain 90 days and 50000 per Promise; global ceilings are 10000 Promises, 10000 operations, 1000000 lifecycle events, and 512 MiB state. SQLite WAL FULL BEGIN IMMEDIATE CAS transactions, deterministic pins and watermarks, capacity-exhausted refusal, and write-ahead action phases are required.
- (correction by Team Argus) Supersede the universal action deadline and hard-reap claim: action concurrency is 4; revalidation 5 seconds; execution defaults are systemd 100, Docker 45, PM2 30, direct process 10, and Launch Mechanism 120 seconds; verification 30 seconds at 500 ms polling; finalization 5 seconds; termination observations 2 plus 1 seconds. Total bounds are derived, and Linux uninterruptible I/O may outlive the durable decision and eventual reaper.
- (decision by Team Argus) Raw argv dispatch reserves canonical config, promise, brief, baseline, action, and ID inspect before legacy action and output profiles. Frozen help, unknown, arity, stream-placement, missing-tool, malformed-output, hostile-identifier, and action-argv cases belong to the compatibility corpus; every target deviation requires a ledger entry.
- (decision) Freeze CollectionPlanV1 in one consistent repository read with generation start, BootIdentity, Promise projection revisions and event sequences, complete PolicySnapshotV1, canonical ScopeManifestV1, and effective obligations; collection and reconciliation use only that cut, preventing mixed-time truth and obligation drift.
- (decision) Make Snapshot commit one BEGIN IMMEDIATE transaction containing scope reports, diagnostics, Observations, materialized Findings, reconciliation input revisions, policy and decision-contract references, and current-pointer CAS; only latest_requested_generation may become current, preventing current truth without its explanation or a split display pointer.
- (decision) Define ScopeIdV1 as the Provider-tagged normalized locator and ScopeManifestV1 as its canonical ordering; use the same values for LPT scheduling, report validation, scope fingerprints, persistence, and baseline compatibility, preventing worker and reducer identity divergence.
- (decision) Define DiagnosticId as generation, ScopeIdV1, and coordinator-assigned canonical ordinal; use an atomic coordinator result registry with half-open deadlines where equality times out, preventing diagnostic remapping and cutoff races.
- (decision) Use latest-wins refresh coalescing: cancel undispatched superseded scopes, request typed cancellation for running superseded scopes, retain their attempt diagnostics, and admit only the newest queued generation; superseded candidates remain historical and never become current.
- (decision) Assign cross-Provider attribution and direct-process deduplication to the reducer after all eligible reports; process workers return bounded candidates and ownership hints, preventing local and global deduplication from producing different Host truth.
- (decision) Give every accepted Promise lifecycle event a gap-free per-Promise sequence and prior projection revision; the projection through sequence N is authoritative and events are audit evidence, preventing replay order from diverging from committed state.
- (decision) Count Host suspend toward Lease and Heartbeat elapsed time on Linux using CLOCK_BOOTTIME semantics; retain wall time only as provenance and expire ephemeral ownership on BootIdentity change, preventing agents from independently selecting suspend-excluding time.
- (decision) Persist immutable ActionPlanV1 records under durable UUIDv7 PlanId with captured SnapshotId, exact target, verb, safety evidence, PolicyFingerprint, decision version, confirmation contract, expiry, actor, and idempotency key; submit consumes by CAS and then allocates OperationId, preventing cross-process plan reconstruction.
- (decision) Define LaunchReceiptV1 and VerificationRequestV1; OperationCoordinator alone applies FR-40 and terminal CAS while adapters emit typed execution evidence, verifier emits correlated evidence, and TUI emits navigation or cancellation requests, preventing competing outcome and launch-boundary owners.
- (decision) Bound terminal restoration and finalization attempts rather than durable completion under unavailable or uninterruptible state I/O; preserve the last durable operation phase for next-start recovery and never claim a hard process-exit or reap bound.
- (decision) Define one complete PolicySnapshotV1 with canonical schema order, base units, canonical JSON encoding, SHA-256 domain-separated PolicyFingerprint excluding provenance, separate provenance digest, and decision_contract_version; historical reads render materialized findings and re-evaluation creates a new generation.
- (decision) Define correlation as deterministic candidate generation, lexicographic evidence vectors, one-to-one assignment, explicit ambiguity ties, and named confidence classes; weak Project, source, launch, name, and process evidence never independently establish healthy or mutation identity.
- (decision) Make hot classification consume retained timestamped resource samples owned by Snapshot history and define state.byte_ceiling as SQLite database plus WAL and SHM allocated bytes plus retained backups and upgrade manifests, preventing incompatible sampling and capacity accounting.
- (decision) Reserve release install, upgrade, validate, status, and rollback as the one-binary release namespace owned by application::release and a StateMigrationCoordinator port; no external installer owns domain or schema decisions.
- (decision) Use an external permission-restricted install-generation lock and fsynced UpgradeTransactionV1 manifest across backup, migration, activation, read-only validation, rollback, and commit; every stateful process holds shared admission and installer takes exclusive quiescence, preventing lost writes and split binary-state recovery.
- (decision) Require StateMigrationCoordinator typed backup, migrate, restore, and verify results with SQLite backup or equivalent checkpointing, sidecar disposition, file and directory fsync, hashes, schema versions, and post-restore integrity, preventing incompatible backup contracts.
- (decision) Resolve Provider executables from configured or compiled absolute allowlisted paths, use a minimal allowlisted environment and fixed neutral working directory, and treat PATH lookup or inherited credentials as invalid in canonical lanes; legacy lanes retain only ledgered compatibility behavior.
- (decision) Preserve legacy action routing by verb prefix before the frozen arity check, make cron explicitly read-only in canonical action capability, and enforce AD-1 with a CI architecture-boundary test before Provider implementation.
- (decision) Define stable HostIdentityV1 from Linux machine identity separately from BootIdentity; define versioned UTF-8 NFC percent encoding and canonical policy and scope fingerprints before released IDs or baseline compatibility become fixtures.
- (decision) Installer preflight inventories every deployed consumer including absolute ExecStart bypasses, requires an explicit disposition for foreign paths, rewrites managed units transactionally, runs daemon-reload and consumer checks, and rolls back links, state, and consumer definitions together.
- (version) Supersede rusqlite 0.40.1 with rusqlite 0.39.0 plus bundled SQLite 3.51.3: the representative locked graph compiles on Rust 1.88, while 0.40.1 resolves libsqlite3-sys 0.38.1 whose cfg_select build script fails on that MSRV; retain Rust 1.88 and verify the exact lockfile in bootstrap CI.
- (version) Pin strsim 0.11.1 for the bounded secondary name-similarity evidence contract; the representative full graph and API probe pass on Rust 1.88.
- (decision) Bind secondary correlation evidence exactly: unequal present Project IDs, same-Provider targets, incompatible sources, or process ownership conflict; name uses strsim 0.11.1 Jaro-Winkler over AD-4 normalization, at most 256 Unicode scalars, with 0.94 or greater as match and lower or over-limit as absent, preventing story-selected similarity algorithms or thresholds.
- (version) Represent the toml lane as crate 1.1.3 with manifest requirement =1.1.3 and TOML specification 1.1.0; Cargo ignores SemVer build metadata in dependency requirements, so +spec-1.1.0 is descriptive registry metadata rather than the manifest pin.
- (version) Pin libc 0.2.186 as the explicit Linux clock boundary for CLOCK_BOOTTIME; the representative Rust 1.88 graph already resolves and compiles this line, avoiding std::time::Instant suspend ambiguity.
- (decision) Extend CollectionPlanV1 with ClockSampleV1, AcceptedBaselineCutV1, OperationCutV1, ResourceHistoryCutV1, and prior-current repository revision; baseline acceptance, operation changes, and retention after admission affect only the next generation, preventing late reads from changing one GenerationId.
- (decision) Make collection admission one BEGIN IMMEDIATE repository operation that allocates the gap-free GenerationId, reads Promise/event/baseline/operation/current/history cuts, inserts the complete CollectionPlanV1, pins its referenced history, and updates latest_requested_generation or commits none of them.
- (decision) Define PolicySnapshot canonical JSON bytes exactly: schema-order keys; NFC scalar strings; only quote and reverse solidus escaped conventionally; C0 controls as uppercase u00XX; slash and every other scalar emitted as UTF-8; invalid scalars rejected; integers minimal decimal; no floats or whitespace; property fixtures own the grammar.
- (decision) Define ScopeIdV1 as a versioned tagged binary grammar with fixed provider tags, big-endian length-framed components and integer fields; Docker endpoint and context use exact NFC strings with no URI aliasing, PM2 and Linux paths use absolute raw-byte lexical normalization, and display percent-encodes the complete canonical bytes.
- (decision) Replace pre-dispatch diagnostic ranges with producer-local DiagnosticCandidateV1 ordering: worker and coordinator producer tags, stable code, canonical subject, source ordinal, canonical parameter bytes, and duplicate occurrence sort into gap-free u32 ordinals after evidence exists; no reducer remap is permitted.
- (decision) Define ProcessOwnershipHintV1 and ProcessSuppressionV1: only exact PID plus birth evidence through self, systemd MainPID/cgroup, Docker init/cgroup, or PM2 PID/birth may suppress direct-process truth; reducer retains all hints, conflicts, completeness, selected owners, rule, and diagnostic, while weak or missing evidence never suppresses.
- (decision) Reserve raw argv __srvls-worker-v1 before public routing and authenticate it over inherited Unix stream FD3 using same-UID parent PID plus same executable device/inode and a one-time capability; stdin/stdout/stderr are not transport and the child performs no config or state discovery.
- (decision) Define WorkerRequestV1 and WorkerResultV1 as one u32-big-endian-length canonical-JSON frame each over FD3 with protocol version, request ID, mode, frozen plan and scope identity, boot deadline, capture reservations, typed payload/report/diagnostics, stable mismatch exits, and no result accepted after timeout or signal.
- (decision) Define ReleaseAdmissionV1 under the XDG srvls upgrade directory with a mode-0600 no-symlink flock path, atomically persisted install generation and ready or recovering state; every stateful entry holds a shared lease and refuses upgrade-recovery-required before SQLite when a nonterminal transaction exists, while release alone takes exclusive recovery ownership.
- (decision) Persist UpgradeTransactionV1 with checksummed canonical bytes through same-directory O_EXCL temp write, file fsync, atomic rename, and directory fsync; each effect writes and fsyncs pending before execution and complete after verification, and recovery treats pending as may-have-executed with an explicit idempotent step rule.
- (decision) Retain exactly one pinned KnownGoodReleaseV1 rollback bundle after successful validation, binding prior binary, state backup, consumer definitions and enablement, hashes, schemas, and install generation; explicit release rollback creates a new UpgradeTransaction from that pair, and only the next successful commit may replace it.
- (decision) Map internal release steps to versioned public phase events stage, checksum, smoke, activate, consumer-validation, recovery, and commit; events persist transaction ID, sequence, started or succeeded or failed or resumed result, stable reason code, and manifest step, and are emitted only after the described durable transition.
- (decision) Require release validation to rewrite every managed absolute ExecStart including srvls-metrics.service and srvls-snapshot.service to the canonical activated binary, read back the loaded path after user daemon-reload, observe each paired timer LastTrigger advance with service Result success and ExecMainStatus zero, or roll back the entire pair.
- (decision) Require ordered SQLite initialization and fail-closed readback on fresh and existing databases: controlled journal_mode WAL returning wal, per-connection synchronous FULL returning 2, foreign_keys ON returning 1 before any transaction, then busy timeout and BEGIN IMMEDIATE.
- (decision) Require release CI to run readelf --version-info on the exact artifact and fail any GLIBC import above GLIBC_2.42, then smoke that artifact in the pinned oldest-supported glibc 2.42 runtime image.
- (decision) Retain boot and UTC wall samples as one ClockSampleV1 at CollectionPlan admission; later wall samples are diagnostic-only and cannot restamp the Snapshot, Evidence Window, or Brief.
- (decision) Update UJ-5 trace ownership to AD-5, AD-16, AD-18, AD-20, and AD-21 with retained resource history; same-binary collection and bounded refresh traces include the new worker-protocol AD.
- (decision) Add property and crash fixtures for canonical JSON and Scope grammars, diagnostic allocation, process suppression, concurrent baseline and operation changes, hot-history retention, atomic plan admission, worker handshake and framing, non-release entry during crashed upgrade, every pending effect, torn manifests, phase-event mapping, timer-triggered consumers, and post-validation rollback.
- (correction) Integrated the acceptance remediation and closed the remaining independent-ready scheduling counterexample: baseline, operation, history, and wall cuts are immutable projections; plan admission is atomic; workers authenticate over FD3 Hello/Ready IPC; Ready dispatch is per-member so a silent or failed peer cannot stall a ready sibling or a later open-gate epoch; transport bytes, evidence cuts, diagnostic allocation, canonical policy/scope grammars, and dedup are explicit; process collection freezes complete roots and proves unrootable-child absence before Host reads; release admission is crash-persistent with ordered journal phases, retained KnownGood rollback, exact public event mapping, SQLite pragma readbacks, readelf ABI proof, and managed timer consumer postconditions.
- (event) Architecture correction on 2026-07-16 remediated FINAL-LOW-01 and FINALPASS-B01: the Structural Seed now assigns authenticated FD3 collection transport only to adapters/worker.rs and FD4 release validation to adapters/release.rs; the event-driven early-free dispatch and unsound full-deadline dominance claim are superseded by one canonical DispatchScheduleV1 compiled from frozen PolicySnapshotV1 and ScopeManifestV1, atomically persisted in CollectionPlanV1, independently rederived and byte-compared by configuration, admission, and runtime, and executed from one pre-spawn schedule origin with immutable worker assignments, reservation offsets, budgets, process-gate intervals, makespan, and cutoff. Early completion, failure, or Ready never advances a reservation; non-process Ready dispatch remains immediate; process waits only for same-epoch parent-side spawn outcomes plus the existing root/absence proof. The default silent-30-second fixture remains a 35-second makespan with 40-second cutoff, and the 20/20/20/20, process-10, cron-9/9/9 one-nanosecond-early fixture cannot start process early and remains a 30-second makespan with 35-second cutoff.
- (correction) TECH-FINAL-01 through TECH-FINAL-04 closed on 2026-07-16 without changing DispatchScheduleV1 or prior release/recovery ordering. This supersedes the stale version entry that named Rust 1.97.0: the official 2026-07-16 stable manifest at https://static.rust-lang.org/dist/channel-rust-stable.toml identifies Rust 1.97.1 and commit 8bab26f4f68e0e26f0bb7960be334d5b520ea452, corroborated by https://blog.rust-lang.org/2026/07/16/Rust-1.97.1/; AD-12 now retains symbolic stable, refreshes official metadata before bootstrap/release compilation, persists manifest date and rustc verbose release/commit identity, and rejects a stale cached compiler. AD-12 and AD-23 now bind manifest-owned normalized service/timer fragment, ExecStart, target, schedule, delay, persistence, wake/reactivation, and exact enablement readback after daemon-reload, then correlate every forward and rollback timer acceptance to baseline LastTriggerUSecMonotonic, InvocationID, and ExecMainStartTimestampMonotonic plus a new nonzero invocation and CLD_EXITED/status-zero success; wrong or disabled-but-active units, already-active/RemainAfterExit targets, and stale success fail. AD-25 now creates the Unix stream pair close-on-exec, retains only the coordinator endpoint and worker FD3 after spawn, closes every original, duplicate, and opposite endpoint before Hello and on every failure, restores FD3 close-on-exec before Provider launch, and requires worker SHUT_WR/close followed by parent clean EOF/close; duplicate-parent-end and duplicate-child-end fixtures make EOF ownership executable. TECH-FINAL-05 remains closed by the unique adapters/worker.rs FD3 and adapters/release.rs FD4 Structural Seed paths.
