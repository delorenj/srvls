# Editorial Prose Review

| Original Text | Revised Text | Changes |
| --- | --- | --- |
| “writes nothing to normal stdout” | “When no machine format is requested, stdout is empty.” | Replaces ambiguous “normal” with the exact output condition. |
| “so no no-animation control is required” | “so v1 needs no separate animation control” | Removes a repeated word and states the scope directly. |
| “human-readable and does not require prose parsing for identity or outcomes” | “human-readable, with labeled identity and outcome fields that do not require inference from prose” | Clarifies that the human surface contains prose while load-bearing fields remain explicit. |
| “with a deterministic nonzero error envelope” | “with a deterministic error envelope and exits nonzero” | Separates envelope content from process exit status. |
| “collector and verification deadlines” | “Collector deadlines and action-verification limits” | Restores canonical capitalization and distinguishes the two Architecture-owned limit classes. |
